# Журнал автономной работы

Здесь фиксируется работа, сделанная без владельца за клавиатурой, и копятся
вопросы к нему. Статусы задач — в [`PLAN.md`](PLAN.md), архитектура — в
[`ARCHITECTURE.md`](ARCHITECTURE.md).

---

## Вопросы к владельцу

Накопились по ходу работы. Ничем не блокируют — везде выбран разумный
вариант по умолчанию, но решение за тобой.

| # | Вопрос | Что сделано пока |
|---|--------|------------------|
| В1 | **Тарифная карта: что бесплатно, что платно?** Нужна и для Фазы 3 (гейтинг), и чтобы решить, какой контент выносить за API — иначе платный доступ ничего не значит | всё открыто, `require_plan` готов, но нигде не навешан |
| В2 | **Ашта-кута: достраивать до 36 баллов?** Не хватает 4 кут (Варна, Вашья, Граха-майтри, Бхакут) — знак Луны для них API отдаёт | считается 21 из 36 с честной пометкой «частичная» |
| В3 | **«Рейтинг дня по сферам» из макета** (работа/деньги/отношения…) — проектировать ли модель? Данных под неё нет вообще | не показываем |
| В4 | **Раху-калам** считается распространённым методом (день на 8 частей). Устраивает или нужен метод конкретной школы? | распространённый + дисклеймер о расхождениях |
| В5 | **Даши**: подтверди, что нужна Вимшоттари (128-летний цикл от накшатры Луны) — она стандарт, но бывают Аштоттари и др. | делаю Вимшоттари |
| В6 | `prototype/dependencies.html` — коммитить в репозиторий? Твой файл, лежит неотслеживаемым | не трогаю |
| В8 | **Имена мухурт**: расчётное ядро и контентная база расходятся в 21 позиции из 30, у восьми — разные божества в одном слоте. Подробности — пункт А10 в листе для астролога | описание берём по номеру, второе имя показываем в разборе |
| В9 | **Оценка мухурты** «благоприятная/неблагоприятная» есть только в расчётном ядре | показываем с подписью «по таблице расчётного ядра» |
| В7 | **Лунные узлы: истинные или средние?** Проект считает истинные (`TRUE_NODE`) — они ретроградны лишь ~75% времени. А `content/retrograde.json` утверждает «Раху и Кету всегда ретроградны». Либо переключить на `MEAN_NODE`, либо поправить текст | флаг отдаём честный, текст не трогаем |

---

## Расхождения в данных и названиях

Технические нестыковки — в отличие от методических вопросов выше, тут ничего
согласовывать с астрологом не нужно, надо просто починить.

| Что | Детали | Статус |
|---|---|---|
| **Названия накшатр расходятся** между API (`utils.NAKSHATRAS[].ru`) и `content/nakshatra_names.json` | 4 позиции из 27: №20 «Пурвашадха» / «Пурва Ашадха», №21 «Уттарашадха» / «Уттара Ашадха», №25 «Пурвабхадра» / «Пурва Бхадрапада», №26 «Уттарабхадра» / «Уттара Бхадрапада» | обойдено сопоставлением по номеру накшатры; напрашивается единый справочник |
| **Расхождение долготы Луны ~33″** с AstroSage при совпавших до 2″ аянамшах | похоже на ΔT; у пограничных рождений может сместить накшатру, а не только даты даш | не разобрано |

---

## Хронология

### 2026-07-25, день — экран карты, проф-глубина, дефект эфемерид

Задача: доработать Фазу 2 до пункта 2.10. К началу дня оставались 2.6
(экран карты), 2.8 (тексты из `content/`) и 2.9 (проф-глубина); 2.10 закрыта
ночью.

Сначала сделан общий механизм, чтобы каждый агент не изобретал свой:
**реестр методических пометок** `frontend-nuxt/utils/method-notes.ts` и
компонент `UiMethodNote`. Спорное место помечается прямо на экране значком
с идентификатором (`А1`, `А5`, `Б7`), по клику — «сейчас так / на
согласовании». Идентификаторы те же, что в `ASTROLOGER-REVIEW.md`.

Отдельно выяснилось, что **у коллеги уже есть своя система пометок**:
`content/disclaimers.json` содержит легенду из трёх меток («классика»,
«школы расходятся», «реконструкция») и 17 секций с типами `verified` /
`needs-check` / `model`. Это оценка автора о надёжности собственных текстов
— другое измерение, чем наши развилки расчёта. Обе системы оставлены, одна
другую не подменяет.

#### 🐛 Систематический сдвиг всех долгот — дефект в расчётном ядре

Агент, делавший карту, сверил её с Prokerala и Serennu и сообщил о
равномерном сдвиге +13″ по всем телам сразу. **Проверил независимо —
подтвердилось.**

`swe.calc_ut` без `FLG_SIDEREAL` возвращает **видимую** тропическую долготу,
то есть уже с нутацией. `swe.get_ayanamsa_ut` возвращает аянамшу **без**
неё. Разность `trop - aya` учитывала нутацию дважды. Величина
знакопеременная: +12,83″ на июнь 1990, −13,93″ на январь 2000, +9,13″ на
сегодня — одинаковая для Солнца, Луны, Сатурна и узлов.

Следствия были шире, чем расхождение с внешними сервисами:

- `nakshatra_calculator.py` и `Panchangam.py` расходились между собой — второй
  давно считает через `FLG_SIDEREAL`, то есть правильно;
- `nakshatra_calendar.py` — те же 13″ в моментах входа Луны в накшатру
  (около 25 секунд времени);
- `sade_sati.py` — Сатурн идёт ~2′ в сутки, поэтому 13″ сдвигали момент смены
  знака почти на три часа, а от него зависят границы Сад-сати.

Починено переходом на `get_ayanamsa_ex_ut` в трёх модулях плюс диагностика в
`/api/debug`. Проверка: расхождение с `FLG_SIDEREAL` стало ровно 0,0000″ по
Солнцу, Луне, Сатурну и узлам на датах 1900–2050; аянамша на 15.06.1990 —
23,727296 вместо 23,723733, что совпадает со Swiss Ephemeris.

Это частично объясняет и записанное ночью расхождение Луны в 33″ с AstroSage
— но только 13″ из них, остаток пока не разобран.

#### ✅ 2.6 — экран карты

`/chart`: раши-чакра в северном и южном начертании по координатам из
`chart_geometry.json`, положения планет с достоинством и ретроградностью,
трактовки домов и знаков, целознаковые дома. Дома, знаки, градусы, накшатры
и пады сверены с Prokerala и Serennu — совпало всё.

Режим «по Луне» решён честно: при недостоверном времени рождения дома не
показываются как факт — либо чандра-лагна с явной пометкой, либо блокировка
с объяснением, если знак Луны сам под вопросом.

**Отличие от исходной версии (пункт Б7).** В `chart_geometry.json` южная
карта индексирована номером дома: в исходнике это была справочная схема без
лагны, дом 1 просто ставился в первую клетку. В настоящей южной карте клетки
закреплены за знаками. Кольцо клеток верное, сдвиг ровно на две позиции —
поправлено в коде, данные не тронуты. Проверил отдельно: соответствие
«знак → клетка» получилось каноническим.

#### ✅ 2.9 — проф-глубина

Наватара-чакра (27 накшатр по девяти тарам в три цикла, подсветка джанмы и
накшатры дня), аспекты и соединения из `yogakarma.json`, текущая маха- и
антар-даша — на `/me`; 30 мухурт с подсветкой текущей — на `/today`. Всё под
проф-режимом, в базовом не грузится вовсе: точка входа бандла не выросла
(182 202 Б против 182 227 Б до правок).

Смещения аспектов агент **разобрал из самих строк данных**, а не задал
таблицей по памяти — правильный ход: пометка «(по ряду школ)» из справочника
доехала до интерфейса, а не потерялась.

Абхиджит на 25.07.2026 для Нью-Дели: у нас 12:00:19–12:54:50, у DrikPanchang
12:00–12:55.

#### 🐛 Страницы утверждали неправду вслед за ушедшим вперёд бэкендом

Ночью закрылись задачи 2.12 и 2.13, а тексты остались прежними:

- `/today` писал «йога и карана не рассчитываются» — хотя API их уже отдаёт.
  Теперь оба элемента на месте, **и появился стоп-фактор Бхадра** (Вишти-карана).
  Это закрывает нарушение доменного правила «панчангу нельзя разрывать»;
- `/me` писал «ретро-статус API не отдаёт» — хотя отдаёт.

Хороший урок: закрывая задачу бэкенда, надо сразу искать в интерфейсе фразы,
которые она делает ложными.

#### ✅ 2.8 — тексты и дисклеймеры из `content/`

Дисклеймеры на всех экранах кабинета читаются из `content/disclaimers.json`
по ключу секции, вводные абзацы — из `ui_texts.json`, добавлен сворачиваемый
блок «Источники» на `/me`. Рядом с каждым дисклеймером видна метка типа
секции — читатель понимает, на чём стоит текст.

**Часть текстов не подошла, и это нормально.** `ui_texts.json` писался под
другой интерфейс: `panchanga_intro` кончается отсылкой «точный расчёт даёт
астрологический сервис» — на странице, которая этот расчёт уже показывает,
звучит как реклама чужого продукта; `chakra_lead`, `chart_full_intro` и
подобные говорят «выберите накшатру / лагну / знаки планет», а у нас данные
приходят из API и выбирать нечего; ещё несколько ссылаются на разделы,
которых в кабинете нет. Оставлены собственные формулировки страниц.

#### 🐛 Пометки достоверности были подписаны неверно — поймал на приёмке

Агент связал типы секций (`verified` / `needs-check` / `model`) с легендой
файла (`классика` / `школы расходятся` / `реконструкция`) **по позиции** и
честно пометил это как реконструкцию. Проверил данные: связи между легендой
и типами в файле нет вообще — легенда описывает пометки внутри самих текстов,
а `type` характеризует секцию целиком. Совпадение «по три элемента» случайно.

Разница смысловая, а не косметическая: тексты секций `needs-check` говорят
«рабочая версия, для проверки с учителем» — это «ещё не сверено», а вовсе не
«традиции дают разные варианты». Подписав одно другим, мы бы приписали автору
утверждение, которого он не делал, — ровно то, чего договорились не делать.

Исправлено: подписи выведены из самих типов («сверено», «рабочая версия»,
«модель»), а легенда автора показывается отдельным списком и целиком.

#### 📌 Новые вопросы к астрологу (внесены в лист под А10–А12)

- **Имена мухурт расходятся у нас же самих**: расчётное ядро и контентная база
  дают разные имена 21 мухурте из 30, причём у восьми это разные божества, а
  не транслитерация (Васу/Савитри, Видхи/Абхиджит, Хутасана/Раухина…). Порядок
  и границы совпадают. Проверено независимо.
- **Оценка мухурты** («благоприятная»/«неблагоприятная») есть только в
  расчётном ядре, в контентной базе её нет.
- **Аспекты только полные и по знакам** — ни дришти-балы, ни орбиса, ни
  частичных аспектов в данных нет.

#### 📌 Что стоит поправить в текстах коллеги (сами не трогали)

- Дисклеймер секции `event-picker` кончается фразой «при интеграции сервис
  применит эту логику к реальным датам» — на живом экране, который её уже
  применяет, читается странно. То же интонационно у `events-muhurta`.
- Дисклеймер на `/pick-date` прямо противоречил коду: утверждал, что правила,
  записанные словом («Амавасья», «Пурнима», «гандантовые»), в арифметику не
  попадают. После ночной починки они попадают — текст переписан по факту.
- В `event-picker.ts` запись «Пурнима (по ряду школ)» обрабатывается как
  обычное совпадение: оговорка школы не различается. Написано на странице
  честно, но стоит подтвердить, что так и надо.
- В `disclaimers.json` ключ `house-info-popup` встречается дважды с разными
  текстами — интерфейс берёт первый.

#### Незакрытое по Фазе 2

- Пометки А5, А6, А7, Б4, Б6 ставить некуда: **экрана совместимости в кабинете
  нет**, хотя ашта-кута полностью посчитана. А1 (нади) временно живёт на `/me`
  и должна переехать туда, когда экран появится.
- Круг 108 пад из `chart_geometry.json` не сделан — нужна навамша (D9),
  которой нет ни в API, ни в калькуляторах.
- Трактовки ретроградности (`content/retrograde.json`) не подключены — показан
  только факт вакри.
- Полное дерево даш (`/api/dasha`) не выведено: на `/me` только текущая пара.

### 2026-07-25, ночь — закрытие пробелов расчётного ядра

Отправная точка: Фаза 2 закрыта по каркасу (оболочка кабинета, `content/` и
`calculators/` подключены, `/me` и `/today` работают на живых данных и
задеплоены). Осталось то, что вскрылось при вёрстке: расчётное ядро не умеет
трёх вещей, и без них экраны неполные.

Порядок выбран по влиянию, а не по номерам: сначала бэкенд (2.11–2.13), потому
что он разблокирует проф-глубину и полноту панчанги, затем оставшиеся экраны.

**В работе (запущено параллельно):**

| Задача | Что делается | Чем проверяется |
|---|---|---|
| 2.12 | Нитья-йога и карана в `Panchangam.py` | сверка с публичной панчангой по датам; Вишти/Бхадра помечается как стоп-фактор |
| 2.13 | Ретроградность планет в `nakshatra_calculator.py` | сверка станций Меркурия с эфемеридами; Солнце/Луна всегда `false`, узлы — по факту |
| 2.11 | Вимшоттари-даши, новый модуль `vimshottari.py` | сверка с публичным калькулятором даш; сумма маха-даш = 120 лет |
| 2.5 | Экран «Подобрать дату» на 33 события | скоринг на реальном профиле, один запрос к API на весь диапазон |

Подключение к `main.py` делаю сам после приёмки — агентам он закрыт, чтобы
не столкнулись правками.

#### ✅ 2.12 — йога и карана посчитаны

`Panchangam.py`: `calc_panchang` теперь возвращает `{"tithis", "yogas", "karanas", "days"}`
(прежние ключи не тронуты). Йога — 27 интервалов по 13°20′ суммы долгот,
карана — 60 полутитх, 11 видов.

**Ловушка, которую поймал агент:** айанамша сокращается в *разности* долгот
(на ней стоят титхи), но не в *сумме* — а йога считается именно от суммы.
Тропические долготы дали бы сдвиг на 2×айанамша ≈ 48°, то есть почти четыре
йоги мимо. Поэтому добавлены отдельные `sun_lon_sid` / `moon_lon_sid`.

Сверка с drikpanchang (Москва, 25–27.07.2026) — совпадение до секунд:

| Граница | Наш расчёт | drikpanchang |
|---|---|---|
| Йога Брахма → Индра | 25.07 18:39:00 | upto 18:39 |
| Карана Вишти → Бава | 25.07 09:04:49 | Vishti upto 09:04 |
| Карана Балава → Каулава | 26.07 11:28:22 | upto 11:27 |

Дополнительно проверено: последовательность 60 каран (7 подвижных ×8 +
4 фиксированные ×1), причём фиксированные выпадают ровно на астрономические
новолуния; непрерывность интервалов без дыр и наложений на 45-дневном
периоде; названия дословно совпали с `content/panchanga.json`, включая
«Вишти (Бхадра)».

Стоп-факторы теперь доступны фронту: у йоги `is_stop` (Вьятипата, Вайдхрити),
у караны `is_bhadra`.

#### ✅ 2.13 — ретроградность планет

`nakshatra_calculator.py`: у каждой планеты теперь `retro`, `speed` (°/сут) и
`is_stationary`. Существующие поля не изменились — сверено побайтово на
30 случайных картах 1900–2050.

Выяснилось, что до правки скорость была недоступна **в принципе**: без флага
`FLG_SPEED` Swiss Ephemeris возвращает на её месте нули, а не производные.

Сверка станций Меркурия (август 2024) — совпадение до минуты:

| Событие | Наш расчёт | Источник |
|---|---|---|
| станция ретроградная | 05.08 04:56 UT, 4°06′ Девы | 05.08 04:56 UT, 4°06′ Девы |
| станция директная | 28.08 21:14 UT, 21°25′ Льва | 28.08 21:14 UT, 21°25′ Льва |

Проверены все три ретро-периода Меркурия 2024 и Сатурн на нашей дате регресса.

**Зачем понадобился `is_stationary`.** Показательно вышло на нашей же тестовой
карте: 17.05.1990 Меркурий стал директным за 4,5 часа до момента рождения.
Флаг `retro: false` там формально верен, но вводит в заблуждение — планета
фактически стоит (0,027°/сут против обычных 1,4). Это классическая вакала,
и без второго признака случай неотличим от нормального прямого хода.
Порог взят относительный (2% от средней скорости планеты), потому что
абсолютный для Сатурна и Меркурия означал бы совершенно разное.

#### ⚠️ Расхождение данных с расчётом — нужно решение (вопрос В7)

Проект использует **истинный** лунный узел (`TRUE_NODE`), а не средний.
Замер за 2024 год: истинный узел ретрограден 274 дня из 366 (75%), в
остальное время идёт прямо — самые длинные окна пришлись на затмения.

При этом в `frontend-nuxt/content/retrograde.json` написано, что «Раху и Кету
всегда ретроградны по своей природе». То есть примерно в четверти случаев
API будет честно отдавать `retro: false`, а контент будет утверждать обратное.

Агент **не стал подделывать флаг** — это было бы выдуманными данными. Варианта
два, и оба законны: переключить узлы на средние (`MEAN_NODE`, тогда «всегда
ретроградны» станет правдой) или поправить формулировку в контенте. Вынес в
вопросы.

#### ✅ 2.5 — экран «Подобрать дату»

`/pick-date`: 33 события, диапазон дат, оценка каждого дня. Два запроса к API
на весь период вместо двух на день; ответы кэшируются по «диапазон + место»,
поэтому смена события пересчитывается локально без сети. Плохие дни показаны
наравне с хорошими и с причиной — день, который советуют избегать, полезнее
молча пропущенного. По клику — построчный разбор факторов и каламы этого дня.

#### 🐛 Найден и починен баг калькулятора — выдавал неверный совет

Агент, делавший экран, наткнулся на дефект в `event-picker.ts`: сверка титхи
шла через `parseInt`, поэтому любая запись, записанная словом, молча
отбрасывалась. Это **42 записи** по всему справочнику, и все они — Амавасья
и Пурнима, две самые значимые титхи.

Конкретно: стрижка на Амавасью получала 73% и вердикт «благоприятна», хотя
в данных эта самая титхи стоит в `avoid_tithis`. То есть сервис советовал
ровно то, от чего справочник предостерегает.

Причина в том, что новолуние и полнолуние — это одна и та же 15-я титхи,
различает их только пакша; числового номера у них в данных нет.

Тем же путём терялась категория «гандантовые» в списках накшатр — она не
совпадала по подстроке ни с одним названием. Развернул через готовую
`isGandantaNakshatra`.

Проверено изолированно (накшатра нейтральна для события, титхи — единственная
переменная): Амавасья и Пурнима теперь роняют стрижку с 73% до 36%, ганданта
распознаётся как шесть накшатр на стыках водных и огненных знаков.

#### ✅ 2.11 — даши Вимшоттари

Новый модуль `vimshottari.py` — чистая функция от долготы Луны и момента
рождения. Подключён так, чтобы вес лёг туда, где он нужен: в `/api/horoscope`
уезжает только **текущий** период (плитка «маха-даша» работает, ответ вырос
до 9 КБ), а полное дерево — 31 КБ на двух уровнях — живёт отдельным
`/api/dasha` и запрашивается по требованию. Сбой даш не роняет гороскоп.

Сверка с AstroSage сделана аккуратно: агент подал калькулятору **его же**
долготу Луны, чтобы сравнивать арифметику даш, а не расхождение эфемерид.
Совпало всё — стартовый управитель, баланс на рождение (4 г 7 мес 9,2 дн
против их 9 дн), длительности всех девяти маха-даш, все девять антар-даш
Раху, состав урезанного первого периода и текущая пара. Дополнительно —
сличение с независимо написанной референс-реализацией: 10 карт, 729
антар-даш, максимальное расхождение 0,001 мс.

**Соглашение о длине года — 365,25 суток.** Развилка реальная: классические
ручные методики считают савана-годом в 360 суток. Выбор обоснован тем, что
длительности AstroSage ложатся ровно на кратные 365,25; вариант с 360
сдвинул бы конец текущего периода Юпитера на полгода, и пользователь при
проверке в стороннем сервисе увидел бы другое.

#### ✅ 2.7 — 243 трактовки «планета в накшатре»

Самый крупный неиспользованный актив контентной базы выведен на `/me` —
для планет самого пользователя. 280 КБ грузятся отдельным чанком, когда до
секции реально доскроллили.

**Сопоставление пришлось делать по номеру накшатры, а не по имени:** API
пишет «Уттарашадха» и «Пурвабхадра», а контент — «Уттара Ашадха» и «Пурва
Бхадрапада». Две из двадцати семи молча не нашлись бы. Номер берётся из
сидерической долготы, имя оставлено запасным путём с нормализацией.

#### 📌 Замечания на будущее

- **Расхождение Луны 33″ с AstroSage** (аянамши при этом совпали до 2″).
  Похоже на ΔT. Само по себе мелочь, но у пограничных рождений это может
  сместить накшатру, а не только даты даш — стоит разобрать отдельно.
- **Названия накшатр расходятся** между API (`utils.NAKSHATRAS`) и
  `content/nakshatra_names.json` в двух позициях. Сейчас обойдено, но
  напрашивается единый справочник.

#### ✅ 2.10 — ашта-кута достроена до 36 баллов

Добавлены четыре недостающие куты: Варна (1), Вашья (2), Граха-майтри (5),
Бхакут (7). `ashtaKutaPartial` не тронута — сверена на всех 729 парах, результат
побайтово тот же. Сверка новых кут с Prokerala на трёх реальных парах: 12 из 12
совпадений, матрица Вашьи проверена поклеточно (23 из 23).

Матрицу Вашьи агент снял с калькулятора, а не скопировал из публикаций —
опубликованные таблицы противоречат друг другу, а самая растиражированная
содержит опечатку: печатает 1.5 там, где её же собственный текст говорит
о половине балла.

Расхождения школ (три клетки Вашьи, натуральная против композитной дружбы,
возврат баллов при отмене бхакут-доши) не спрятаны — едут в результате, чтобы
UI мог их показать. Оговорка о неполноте ашта-куты возвращается всегда, включая
пункт, что возраст, здоровье, договорённости и согласие людей лежат вне джьотиша.

#### 🐛 Найден дефект в самой дорогой куте — и он оказался в коде, а не в данных

Агент сообщил, что нади неверна у девяти накшатр в `content/nakshatras.json`,
и предложил править данные. **Проверил сам — данные оказались в порядке.**

Структурное поле `dosha` верно на все 27 (сверено с канонической схемой
Ади-Мадхья-Антья / Антья-Мадхья-Ади). Оно записано в аюрведической
номенклатуре — Вата, Питта, Капха, — которая соответствует нади один в один.

Ошибка была в другом месте: `nadiOf` вытаскивал нади **регулярным выражением
из свободного текста** `compat`. Вот эта проза действительно врёт у девяти
накшатр и ещё у шести пишет «Анта/Антья», из-за чего строковое сравнение не
находило совпадений и нади-доша терялась.

Нади — самая дорогая кута, 8 баллов из 36. Замер влияния: **230 пар из 729
(32%) считались неверно**, причём в 108 случаях доша не находилась вовсе — то
есть сервис сообщал об отсутствии препятствий там, где традиция видит серьёзное.

Починено в коде, контент коллеги не тронут. Хороший довод в пользу того, чтобы
проверять выводы агентов до того, как править по ним данные.

---

## Итог ночной работы

Закрыты все задачи, что были в списке Фазы 2: 2.5, 2.7, 2.10, 2.11, 2.12, 2.13.

Попутно найдены и починены **два тихих дефекта**, каждый из которых выдавал
пользователю неверный результат, не падая и никак себя не выдавая:
1. подбор даты игнорировал 42 правила справочника (Амавасья, Пурнима, ганданта);
2. расчёт совместимости терял нади-дошу в трети пар.

Оба — не в данных, а в способе их чтения. Данные были правильные.
